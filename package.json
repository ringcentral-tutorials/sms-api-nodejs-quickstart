{
  "name": "sms-api-nodejs-quickstart",
  "version": "0.1.0",
  "license": "MIT",
  "description": "A quickstart tutorial to teach users to use RingCentral SMS API.",
  "scripts": {
    "build": "cd docs && ../node_modules/.bin/saurus author",
    "docs": "cd docs && npx saurus author",
    "upgrade": "yarn-upgrade-all"
  },
  "dependencies": {
    "dotenv": "^5.0.1",
    "form-data": "^2.3.2",
    "ringcentral": "^3.2.0"
  },
  "devDependencies": {
    "standard": "^11.0.1",
    "viewsaurus-ringcentral": "^0.4.7",
    "yarn-upgrade-all": "^0.3.0"
  }
}
